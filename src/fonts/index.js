import { Outfit } from 'next/font/google'

export const Outfit300 = Outfit({
  subsets: ['latin'],
  weight: ['300'],
  display: 'swap',
})

export const Outfit400 = Outfit({
  subsets: ['latin'],
  weight: ['400'],
  display: 'swap',
})

export const Outfit500 = Outfit({
  subsets: ['latin'],
  weight: ['500'],
  display: 'swap',
})

export const Outfit600 = Outfit({
  subsets: ['latin'],
  weight: ['600'],
  display: 'swap',
})

export const Outfit700 = Outfit({
  subsets: ['latin'],
  weight: ['700'],
  display: 'swap',
})
